{
  "name": "pleitost-session-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Servidor de sessão do companion pleitost — auth GitHub (device flow) + salas por código + sync WebSocket",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
